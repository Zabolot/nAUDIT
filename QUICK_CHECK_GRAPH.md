# ⚡ БЫСТРАЯ ПРОВЕРКА: Граф работает?

## За 2 минуты

```powershell
# 1️⃣ Запустить exe (30 сек)
dist\nAUDIT.exe

# 2️⃣ В окне: File → Open Project (30 сек)
# Выбрать любую папку с Python файлами

# 3️⃣ Нажать "Start Audit" (дождаться завершения)

# 4️⃣ Открыть вкладку "🌳 Ошибки" (5 сек)

# 5️⃣ Нажать кнопку "🕸️  Граф" (5 сек)

# 6️⃣ Посмотреть результат
```

## Ожидаемые результаты

| Результат | Статус | Что значит |
|-----------|--------|-----------|
| 📊 Граф с узлами | ✅ OK | Исправилось! |
| 📊 "Граф пуст" | ✅ OK | Нет ошибок в проекте |
| ⚪ Белый экран | ❌ Ошибка | Проблема остается |

## Если белый экран - запустить с отладкой

```powershell
# Активировать окружение
.\v.naudit\Scripts\Activate.ps1

# Запустить с логами
python run_naudit_debug.py 2>&1 | Tee-Object debug.log

# Повторить шаги выше (File → Open → Audit → Errors → Graph)
# Отправить содержимое debug.log
```

## Дополнительно

**Для нетерпеливых - запустить тесты:**
```powershell
.\v.naudit\Scripts\Activate.ps1
python test_graph_populate.py
```

**Должно быть:**
```
SUCCESS: 3 nodes loaded
Graph will display:
  - src/auth.py (1 errors)
  - src/main.py (1 errors)
  - src/utils.py (1 errors)
```

---

**Готово!** Если видите граф - проблема исправлена ✅
