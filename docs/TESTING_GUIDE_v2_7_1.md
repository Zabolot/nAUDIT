# РУКОВОДСТВО ПО ТЕСТИРОВАНИЮ ИСПРАВЛЕНИЙ v2.7.1

**Дата:** 16 ноября 2025  
**Версия:** nAUDIT v2.7.1  
**Статус:** Готово к тестированию

---

## БЫСТРЫЙ СТАРТ

### 1️⃣ Проверить что exe собралась

```bash
dir dist\nAUDIT.exe
```

**Ожидаемо:**
- Файл должен существовать
- Размер: ~1.0-1.2 GB
- Дата: сегодняшняя

### 2️⃣ Запустить exe

```bash
.\dist\nAUDIT.exe
```

**Ожидаемо:**
- Окно приложения откроется за 5-10 секунд
- Показать главное окно с 6 вкладками
- Никаких ошибок в консоли

---

## ДЕТАЛЬНОЕ ТЕСТИРОВАНИЕ

### ТЕСТ 1: GPU Detection ✅

#### Шаг 1: Загрузка приложения
1. Запустить exe: `.\dist\nAUDIT.exe`
2. Подождать загрузку приложения

#### Шаг 2: Проверка GPU статуса
1. Открыть вкладку "🔧 Инструменты" (если есть)
2. Искать информацию о GPU
3. **Ожидаемо:**
   - Если GPU есть: `GPU Available: True` + информация о GPU
   - Если GPU нет: `GPU Available: False` (нормально)

#### Шаг 3: Проверка логов
1. Открыть файл: `%USERPROFILE%\.naudit\logs\latest.log`
2. Искать строки:
   ```
   [GPU Detection] PyTorch версия: 2.x.x
   [GPU Detection] CUDA доступна: True/False
   ```

**Критерий успеха:** ✅ Логи содержат информацию о GPU detection процессе

---

### ТЕСТ 2: Tree Widget Error Display 🌳

#### Шаг 1: Выбрать проект с ошибками
1. Нажать "📂 Выбрать папку" в интерфейсе
2. Выбрать папку с Python кодом который содержит ошибки
   - Пример: папка с test_project_with_errors/
   - Или любая папка с .py файлами

#### Шаг 2: Запустить аудит
1. Нажать "🚀 Начать аудит" (или "Запустить" и т.п.)
2. Подождать завершения (прогресс бар)

#### Шаг 3: Проверить дерево ошибок
1. Перейти на вкладку "🌳 Ошибки" (или "Errors", "Tree" и т.п.)
2. **Ожидаемо:**
   - Видно иерархическое дерево файлов
   - Папки (📁) раскрыты автоматически
   - Файлы с ошибками (📄) отмечены количеством ошибок
   - Ошибки могут быть подсвечены цветом (красный/оранжевый)

#### Шаг 4: Развернуть файл с ошибками
1. Клик на файл с ошибками в дереве
2. **Ожидаемо:**
   - Справа показываются все ошибки в файле
   - Список содержит: строка, тип ошибки, сообщение
   - Если развернуть - показываются детали

**Критерий успеха:** ✅ Ошибки видны в дереве и раскрываются при клике

---

### ТЕСТ 3: Tree-Graph Synchronization 🔗

#### Шаг 1: Открыть режим SPLIT (Оба)
1. В вкладке "🌳 Ошибки" нажать кнопку "📊 Оба"
2. **Ожидаемо:**
   - Левая часть: дерево файлов
   - Правая часть: граф проекта
   - Оба одновременно видны

#### Шаг 2: Синхронизация дерево → граф
1. **В дереве:** Клик на файл с ошибками
2. **Ожидаемо:**
   - В графе справа: узел файла должен быть выделен/подсвечен
   - Граф должен сфокусироваться на этом узле
   - Узел получает выделение (другой цвет/размер)

#### Шаг 3: Синхронизация граф → дерево
1. **В графе:** Клик на узел файла (кружочек с номером)
2. **Ожидаемо:**
   - В дереве слева: соответствующий файл должен быть выделен
   - Дерево должно прокрутиться к этому файлу
   - Папка автоматически раскроется если закрыта

#### Шаг 4: Разные режимы
1. Переключиться на режим "🌳 Дерево" только
2. Клик на файл - должно работать
3. Переключиться на режим "🕸️ Граф" только
4. Клик на узел - должно работать

**Критерий успеха:** ✅ Выбор в дереве ↔ выделение в графе работает синхронно

---

## КОМПЛЕКСНЫЙ ТЕСТ (все вместе)

1. ✅ Запустить exe
2. ✅ Проверить GPU статус
3. ✅ Выбрать проект
4. ✅ Запустить аудит
5. ✅ Проверить ошибки в дереве
6. ✅ Проверить синхронизацию дерево ↔ граф
7. ✅ Проверить разные режимы просмотра
8. ✅ Клик на несколько файлов - синхронизация должна работать
9. ✅ Переключение между режимами - функциональность сохраняется

---

## ПРОВЕРКА ЛОГОВ

### Где найти логи
```
%USERPROFILE%\.naudit\logs\latest.log
```

### Что ищем в логах

**GPU Detection:**
```
[GPU Detector] PyTorch версия: 2.x.x
[GPU Detector] CUDA доступна: True/False
[GPU Detector] ✓ GPU обнаружена: NVIDIA GeForce RTX ...
```

**Tree Widget:**
```
[Tree Widget] Starting populate_from_report: /path/to/project
[Tree Widget] Found code_issues at report.metrics.code_issues: 15 items
[Tree Widget] _build_file_tree called
[Tree Widget] Building tree for 8 files
[Tree Widget] Tree items after build: 8
```

**Synchronization:**
```
[ErrorVisualizationWidget] 🔗 Синхронизация: дерево → граф (src/main.py)
[ErrorVisualizationWidget] ✅ Файл выделен в дереве: src/main.py
[GraphVisualizer] ✅ Выделен файл: src/main.py
```

---

## СЦЕНАРИИ ТЕСТИРОВАНИЯ

### Сценарий 1: Проект без ошибок
**Действие:** Выбрать пустую папку или папку без ошибок  
**Ожидаемо:**
- Сообщение: "✓ Анализ завершён - ошибок не найдено!"
- Дерево пусто или показывает одно корневое сообщение
- Граф показывает только структуру файлов

### Сценарий 2: Проект с много ошибками (100+)
**Действие:** Выбрать большой проект с много ошибками  
**Ожидаемо:**
- Дерево корректно отображает все ошибки
- Нет зависания при открытии
- Синхронизация работает плавно
- Логи показывают время обработки

### Сценарий 3: Быстрое переключение между файлами
**Действие:** Быстро клик-клик-клик на разные файлы в дереве  
**Ожидаемо:**
- Граф обновляется для каждого выбора
- Нет задержек или зависаний
- Логи показывают все переключения

---

## ОТЧЕТ О РЕЗУЛЬТАТАХ

**Используй этот чек-лист:**

```
ТЕСТ 1 - GPU Detection
[ ] GPU статус отображается корректно
[ ] Логи содержат информацию о GPU detection
[ ] Нет ошибок при запуске

ТЕСТ 2 - Tree Widget Error Display
[ ] Ошибки видны в дереве
[ ] Папки раскрыты автоматически
[ ] Количество ошибок показано правильно
[ ] Цвета файлов соответствуют серьёзности

ТЕСТ 3 - Tree-Graph Synchronization
[ ] Клик в дереве → выделение в графе
[ ] Клик в графе → выделение в дереве
[ ] Работает в режиме "Дерево" только
[ ] Работает в режиме "Граф" только
[ ] Работает в режиме "Оба" (Split)
[ ] Нет задержек при переключении

ОБЩЕЕ
[ ] Приложение не падает
[ ] Нет критических ошибок
[ ] Логи информативные
[ ] Интерфейс отзывчив
```

---

## УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: Exe не запускается
**Решение:**
1. Проверить что exe существует: `dir dist\nAUDIT.exe`
2. Пересобрать: `python -m PyInstaller --onefile --windowed --name nAUDIT run_naudit_gui.py`

### Проблема: Ошибки не видны в дереве
**Решение:**
1. Проверить логи: какие ошибки найдены?
2. Убедиться что выбран правильный режим просмотра
3. Попробовать другой проект

### Проблема: Нет синхронизации между деревом и графом
**Решение:**
1. Попробовать режим "Оба" (Split)
2. Проверить что сигналы испускаются (в логах)
3. Пересобрать exe

### Проблема: GPU показывает False хотя GPU есть
**Решение:**
1. Проверить логи для деталей
2. Запустить nvidia-smi в терминале вручную
3. Убедиться что torch установлена в exe

---

## РЕЗУЛЬТАТЫ ОЖИДАЕМЫЕ

✅ **GPU Detection:** Работает, логирует процесс, корректно определяет GPU  
✅ **Tree Display:** Ошибки видны, папки раскрыты, структура правильная  
✅ **Synchronization:** Двусторонняя работает, все режимы поддержаны  
✅ **Performance:** Нет зависаний, плавное переключение, логи информативны  

---

**Если все тесты пройдены - 🎉 РЕЛИЗ ГОТОВ!**
